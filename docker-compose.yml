services:
  srt-generator:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - uploads:/app/server/uploads
      - srt-output:/app/server/srt-output
    restart: unless-stopped

volumes:
  uploads:
  srt-output:
